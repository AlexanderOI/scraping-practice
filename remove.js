const marca = document.querySelectorAll('.product-brand');
marca.forEach(element => {
  element.remove();
});

const descuento = document.querySelectorAll('.oldproductPrice');
descuento.forEach(element => {
  element.remove();
});
